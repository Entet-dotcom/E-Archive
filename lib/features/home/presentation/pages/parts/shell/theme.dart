part of '../../dashboard_shell_page.dart';

// Shared admin palette (top-level so `part` files can use it).
const _adminBg = Color(0xFF0B1220);
const _adminSidebar = Color(0xFF141D2E);
const _adminBorder = Color(0xFF1F2937);
const _adminText = Color(0xFFE5E7EB);
const _adminMuted = Color(0xFF94A3B8);